{
  "properties": {
    "development_system":{
      "description": "Structure that contains all the information needed to send a packet to development system",
      "type": "object",
      "properties": {
        "ip": {
          "description": "String containing the ip of the Development system",
          "type": "string"
        },
        "port": {
          "description": "number containing the port of the Development system",
          "type": "number"
        }
      },
      "required": ["ip","port"]
    },
    "session_nr_threshold": {
      "description": "number representing the amount of sessions needed to be able to generate the training dataset",
      "type": "number"
    },
    "development_mode": {
      "description": "number representing the current mode of execution: 0 -> development, 1 -> execution",
      "type": "string"
    }
  },
  "required": ["development_system","session_nr_threshold", "development_mode"]
}