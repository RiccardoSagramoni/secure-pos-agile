CREATE TABLE expertLabel (session_id TEXT PRIMARY KEY UNIQUE, value TEXT)
CREATE TABLE classifierLabel (session_id TEXT PRIMARY KEY UNIQUE, value TEXT)

SELECT expert.session_id, expert.value as expertValue, classifier.value as classifierValue
FROM expertLabel AS expert INNER JOIN classifierLabel AS classifier
ON expert.session_id = classifier.session_id

DELETE FROM classifierLabel
DELETE FROM expertLabel