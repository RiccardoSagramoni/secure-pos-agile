{
   "title": "Received JSON",
   "description": "Schema for the received json",
   "type": "object",

   "properties": {

      "result": {
         "description": "field for demo execution",
         "type": "string"
      },
      "test": {
         "description": "field for demo execution",
         "type": "string"
      }
   },

   "required": ["result"]
}